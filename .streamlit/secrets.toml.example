# Copy this content into the Streamlit Community Cloud "Secrets" editor
# (App settings → Secrets) — do NOT commit the real secrets.toml to git.

# URL of your deployed Render FastAPI backend
API_BASE = "https://migrainetackler-api.onrender.com"
